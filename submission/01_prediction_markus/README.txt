MarkUs — Prediction assignment
================================

Upload these 3 files from THIS folder (after running sync_submission.py):

  - pred.py
  - model_state.json
  - model_weights.npz

Regenerate the two artifacts from repo root:
  python export_model.py

Then:
  python submission/sync_submission.py

Imports allowed in pred.py: stdlib, numpy, pandas only (no sklearn).
