# Report plots (`plots/`)

Generated by `python report_figures.py` from the repository root. Each PNG has a **short multi-line label under the plot** (Figure N, what it shows, split line, then **Seed 42** on its own line where applicable). Figure 4 uses multiple seeds listed in the label.

| File | Short description |
|------|-------------------|
| `01_model_compare_test.png` | Test accuracy and macro-F1 for LR, NB, RF, majority vote, stacking. |
| `02_confusion_matrix_stacking.png` | Confusion matrix for stacking on the test set. |
| `03_errors_true_starry_night.png` | *Starry Night* rows: shows most are correct; blue bars = few confusions and target class. |
| `04_partition_seed_sensitivity.png` | Stacking test accuracy vs six random seeds (80/20 split protocol). |
| `05_appendix_model_compare_min_mean_max.csv` | Numeric min/mean/max (model A; add B/C with `--appendix`). |
| `05_appendix_model_compare.png` | (Optional) Bar chart of min/mean/max if you run with `--appendix`. |

For LaTeX, copy or paraphrase the title lines from the figure into `\caption{...}`.
