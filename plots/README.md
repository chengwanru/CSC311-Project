# Report plots (`plots/`)

Generated by `python report_figures.py` from the repository root. Each PNG has a **short multi-line title** at the top (Figure N + what the plot shows + key settings such as split seed). Long captions were removed so exports are not stretched horizontally.

| File | Short description |
|------|-------------------|
| `01_model_compare_test.png` | Test accuracy and macro-F1 for LR, NB, RF, majority vote, stacking. |
| `02_confusion_matrix_stacking.png` | Confusion matrix for stacking on held-out test. |
| `03_errors_true_starry_night.png` | Distribution of predictions when true class is *The Starry Night*. |
| `04_partition_seed_sensitivity.png` | Stacking test accuracy vs six RNG seeds for shuffling persons before 80/20 split. |
| `05_appendix_model_compare_min_mean_max.csv` | Numeric min/mean/max (model A; add B/C with `--appendix`). |
| `05_appendix_model_compare.png` | (Optional) Bar chart of min/mean/max if you run with `--appendix`. |

For LaTeX, copy or paraphrase the title lines from the figure into `\caption{...}`.
