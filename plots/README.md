# Report plots (`plots/`)

Generated by `python report_figures.py` from the repository root. Each PNG has a **short multi-line caption** in a tight strip under the axes.

| File | Short description |
|------|-------------------|
| `01_model_compare_test.png` | Test accuracy and macro-F1 for LR, NB, RF, majority vote, stacking. |
| `02_confusion_matrix_stacking.png` | Test-set confusion matrix (counts). |
| `03_errors_true_starry_night.png` | *Starry Night* true-class rows: predicted-label counts (red = correct). |
| `04_partition_seed_sensitivity.png` | Test accuracy vs six random partition seeds (80/20 protocol). |
| `05_appendix_model_compare_min_mean_max.csv` | Numeric min/mean/max (model A; add B/C rows with `--appendix`). |
| `05_appendix_model_compare_min_mean_max.png` | Same numbers as the CSV; PNG table is **Min / Mean / Max** only (no model column) plus caption. |
| `06_confusion_matrix_baselines.png` | One figure with LR/NB/RF/Majority confusion matrices (same test split as Fig. 1). |
| `07_stacking_train60_vs_trainval80.png` | Stacking test accuracy: OOF+refit on **60% train** vs **80% train+val** (same 20% test, six seeds). |
| `05_appendix_model_compare.png` | Optional bar chart if you run with `--appendix`. |

For LaTeX, copy or paraphrase the title lines from the figure into `\caption{...}`.
